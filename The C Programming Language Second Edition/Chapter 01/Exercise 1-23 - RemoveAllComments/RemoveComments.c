#include "stdio.h"

int MAX_INPUT = 1000;

void get_input(char output[], int output_length);

int main()
{
    return 0;
}

/* Reads input until EoF or output_length reached and stores it in output */
void get_input(char output[], int output_length)
{
    int c;
    for (int i = 0; i < output_length; i++)
    {
        
    }
}